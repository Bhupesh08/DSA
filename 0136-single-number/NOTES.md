​Bit manipulation is used/can also be used.

Remember how u learnt that the XOR operation is used when we need to find a single element when other elements are given multiple no. of times (even number).
